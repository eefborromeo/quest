<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>Quest</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="font-source m-auto container h-full">
    <nav class="p-5 flex justify-between items-center">
      <a href="/">
        <h1 class="text-5xl font-black">Quest</h1>
      </a>
      <div>
        <% if user_signed_in? %>
          <%= link_to 'Edit Profile', edit_user_registration_path, class: "rounded-lg py-3 px-5 bg-gray-100 inline font-medium"%>
          <%= button_to 'Log Out', destroy_user_session_path, method: :delete, class: "rounded-lg py-3 px-5 bg-gray-100 inline font-medium"%>
        <% else %>
          <%= link_to 'Sign Up', new_user_registration_path, class: "rounded-lg py-3 px-5 bg-gray-100 inline font-medium"%>
          <%= link_to 'Login', new_user_session_path, class: "rounded-lg py-3 px-5 bg-gray-100 inline font-medium"%>
        <% end %>
      </div>
    </nav>
    <main class="px-10">
    <% if notice.present? %>
        <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>
      <%= yield %>
    </main>
  </body>
</html>
